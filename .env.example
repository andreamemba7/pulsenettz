# Copy to .env.local for `vercel dev`. NEVER commit the real values —
# in production these live in Vercel → Settings → Environment Variables.

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
TO_EMAIL=pulsenettz@gmail.com
FROM_EMAIL=website@pulsenettz.com
